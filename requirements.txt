# requirements.txt

# --- Top-level libraries my app uses ---
flask
python-dotenv
gunicorn
requests
langchain-neo4j
huggingface-hub==0.33.1   # Keeping this pin is ok if you need it, but you could loosen it.
neo4j-graphrag==1.7.0   # This pin is fine.
transformers
openai
torch
Pillow
# ... other direct dependencies

# --- Development libraries ---
pytest

# NOTICE: fsspec, pyyaml, typing-extensions, etc. are NOT listed here.
# Pip will install the correct versions of them based on the needs of the libraries above.